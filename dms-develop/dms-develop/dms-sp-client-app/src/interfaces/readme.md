Use intefaces to describe the model and services. Ex:
    - IDmsDocument
    - IDmsApprovalService

Same as components, cross-component services and entities should be here, if they are specific to a component they stay at the component level.
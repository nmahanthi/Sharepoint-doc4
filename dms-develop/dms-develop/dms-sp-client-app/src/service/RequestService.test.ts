import { describe } from '@jest/globals';


// const settings = require('../test-settings.json')
describe('ReferenceService', () => {

    // test('getLastIndex - Should return 0 if no previous documents in category', async () => {
    //     const index = await refService.getLastIndex('unit-test');
    //     expect(index).toBe(0);
    // });
});